Dockerfile
    FROM node:alpine
    COPY . /app
    WORKDIR /app
    CMD node /app.js

In the command prompt
    1. docker build -t "hello-docker" .
        - will build the image
    2. docker image ls
        - will list down the list of images created in docker
